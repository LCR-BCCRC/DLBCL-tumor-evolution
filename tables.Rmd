# Tables


<!-- Add a new code chunk for each table you want to add. 
Give each chunk a convenient generic name so you can easily call it from the text. 
Tweak styling if necessary. These tables won't work in the Word/docx outputs but functionality for that option would be great to add if someone knows how -->

```{r table1, echo=F, include=T,message=F}

iris2 <- head(iris)
#if(grepl("pdf",output_format)){
#    knitr::kable(iris2, align = "lccrr",caption = "An example table caption.") %>% 
#    kableExtra::kable_styling(latex_options = "hold_position")
#}else{
    knitr::kable(iris2,caption="a caption")
#}
#WHY DOESN'T THIS WORK TO FORCE LATEX OPTIONS WHEN KNITTING TO PDF?!
```

<!-- You may or may not want to duplicate the \newpage line after every table (to force each table to its own page)-->

\newpage